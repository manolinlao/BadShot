services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: badshot
      POSTGRES_PASSWORD: badshot
      POSTGRES_DB: badshot
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
